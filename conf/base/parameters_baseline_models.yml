baseline_models:
  test_size: 0.2
  random_state: 42
  target_col: "Heart Disease"
  logit_formula: "Q('Heart Disease') ~ Age + Sex + Q('Chest pain type') + BP + Cholesterol + Q('FBS over 120') + Q('EKG results') + Q('Max HR') + Q('Exercise angina') + Q('ST depression') + Q('Slope of ST') + Q('Number of vessels fluro') + Thallium"

baseline_models_v1:
  test_size: 0.2
  random_state: 42
  target_col: "Heart Disease"
  logit_formula: "Q('Heart Disease') ~ HR_Deficit + Q('Chest pain type') + Thallium"
